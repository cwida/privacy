SELECT as_noised_sum(as_key, ResolutionWidth) AS sw0,
       as_noised_sum(as_key, ResolutionWidth + 1) AS sw1,
       as_noised_sum(as_key, ResolutionWidth + 2) AS sw2,
       as_noised_sum(as_key, ResolutionWidth + 3) AS sw3,
       as_noised_sum(as_key, ResolutionWidth + 4) AS sw4,
       as_noised_sum(as_key, ResolutionWidth + 5) AS sw5,
       as_noised_sum(as_key, ResolutionWidth + 6) AS sw6,
       as_noised_sum(as_key, ResolutionWidth + 7) AS sw7,
       as_noised_sum(as_key, ResolutionWidth + 8) AS sw8,
       as_noised_sum(as_key, ResolutionWidth + 9) AS sw9,
       as_noised_sum(as_key, ResolutionWidth + 10) AS sw10,
       as_noised_sum(as_key, ResolutionWidth + 11) AS sw11,
       as_noised_sum(as_key, ResolutionWidth + 12) AS sw12,
       as_noised_sum(as_key, ResolutionWidth + 13) AS sw13,
       as_noised_sum(as_key, ResolutionWidth + 14) AS sw14,
       as_noised_sum(as_key, ResolutionWidth + 15) AS sw15,
       as_noised_sum(as_key, ResolutionWidth + 16) AS sw16,
       as_noised_sum(as_key, ResolutionWidth + 17) AS sw17,
       as_noised_sum(as_key, ResolutionWidth + 18) AS sw18,
       as_noised_sum(as_key, ResolutionWidth + 19) AS sw19,
       as_noised_sum(as_key, ResolutionWidth + 20) AS sw20,
       as_noised_sum(as_key, ResolutionWidth + 21) AS sw21,
       as_noised_sum(as_key, ResolutionWidth + 22) AS sw22,
       as_noised_sum(as_key, ResolutionWidth + 23) AS sw23,
       as_noised_sum(as_key, ResolutionWidth + 24) AS sw24,
       as_noised_sum(as_key, ResolutionWidth + 25) AS sw25,
       as_noised_sum(as_key, ResolutionWidth + 26) AS sw26,
       as_noised_sum(as_key, ResolutionWidth + 27) AS sw27,
       as_noised_sum(as_key, ResolutionWidth + 28) AS sw28,
       as_noised_sum(as_key, ResolutionWidth + 29) AS sw29,
       as_noised_sum(as_key, ResolutionWidth + 30) AS sw30,
       as_noised_sum(as_key, ResolutionWidth + 31) AS sw31,
       as_noised_sum(as_key, ResolutionWidth + 32) AS sw32,
       as_noised_sum(as_key, ResolutionWidth + 33) AS sw33,
       as_noised_sum(as_key, ResolutionWidth + 34) AS sw34,
       as_noised_sum(as_key, ResolutionWidth + 35) AS sw35,
       as_noised_sum(as_key, ResolutionWidth + 36) AS sw36,
       as_noised_sum(as_key, ResolutionWidth + 37) AS sw37,
       as_noised_sum(as_key, ResolutionWidth + 38) AS sw38,
       as_noised_sum(as_key, ResolutionWidth + 39) AS sw39,
       as_noised_sum(as_key, ResolutionWidth + 40) AS sw40,
       as_noised_sum(as_key, ResolutionWidth + 41) AS sw41,
       as_noised_sum(as_key, ResolutionWidth + 42) AS sw42,
       as_noised_sum(as_key, ResolutionWidth + 43) AS sw43,
       as_noised_sum(as_key, ResolutionWidth + 44) AS sw44,
       as_noised_sum(as_key, ResolutionWidth + 45) AS sw45,
       as_noised_sum(as_key, ResolutionWidth + 46) AS sw46,
       as_noised_sum(as_key, ResolutionWidth + 47) AS sw47,
       as_noised_sum(as_key, ResolutionWidth + 48) AS sw48,
       as_noised_sum(as_key, ResolutionWidth + 49) AS sw49,
       as_noised_sum(as_key, ResolutionWidth + 50) AS sw50,
       as_noised_sum(as_key, ResolutionWidth + 51) AS sw51,
       as_noised_sum(as_key, ResolutionWidth + 52) AS sw52,
       as_noised_sum(as_key, ResolutionWidth + 53) AS sw53,
       as_noised_sum(as_key, ResolutionWidth + 54) AS sw54,
       as_noised_sum(as_key, ResolutionWidth + 55) AS sw55,
       as_noised_sum(as_key, ResolutionWidth + 56) AS sw56,
       as_noised_sum(as_key, ResolutionWidth + 57) AS sw57,
       as_noised_sum(as_key, ResolutionWidth + 58) AS sw58,
       as_noised_sum(as_key, ResolutionWidth + 59) AS sw59,
       as_noised_sum(as_key, ResolutionWidth + 60) AS sw60,
       as_noised_sum(as_key, ResolutionWidth + 61) AS sw61,
       as_noised_sum(as_key, ResolutionWidth + 62) AS sw62,
       as_noised_sum(as_key, ResolutionWidth + 63) AS sw63,
       as_noised_sum(as_key, ResolutionWidth + 64) AS sw64,
       as_noised_sum(as_key, ResolutionWidth + 65) AS sw65,
       as_noised_sum(as_key, ResolutionWidth + 66) AS sw66,
       as_noised_sum(as_key, ResolutionWidth + 67) AS sw67,
       as_noised_sum(as_key, ResolutionWidth + 68) AS sw68,
       as_noised_sum(as_key, ResolutionWidth + 69) AS sw69,
       as_noised_sum(as_key, ResolutionWidth + 70) AS sw70,
       as_noised_sum(as_key, ResolutionWidth + 71) AS sw71,
       as_noised_sum(as_key, ResolutionWidth + 72) AS sw72,
       as_noised_sum(as_key, ResolutionWidth + 73) AS sw73,
       as_noised_sum(as_key, ResolutionWidth + 74) AS sw74,
       as_noised_sum(as_key, ResolutionWidth + 75) AS sw75,
       as_noised_sum(as_key, ResolutionWidth + 76) AS sw76,
       as_noised_sum(as_key, ResolutionWidth + 77) AS sw77,
       as_noised_sum(as_key, ResolutionWidth + 78) AS sw78,
       as_noised_sum(as_key, ResolutionWidth + 79) AS sw79,
       as_noised_sum(as_key, ResolutionWidth + 80) AS sw80,
       as_noised_sum(as_key, ResolutionWidth + 81) AS sw81,
       as_noised_sum(as_key, ResolutionWidth + 82) AS sw82,
       as_noised_sum(as_key, ResolutionWidth + 83) AS sw83,
       as_noised_sum(as_key, ResolutionWidth + 84) AS sw84,
       as_noised_sum(as_key, ResolutionWidth + 85) AS sw85,
       as_noised_sum(as_key, ResolutionWidth + 86) AS sw86,
       as_noised_sum(as_key, ResolutionWidth + 87) AS sw87,
       as_noised_sum(as_key, ResolutionWidth + 88) AS sw88,
       as_noised_sum(as_key, ResolutionWidth + 89) AS sw89
FROM (
    SELECT ResolutionWidth, priv_hash(hash(rowid)) AS as_key
    FROM hits
) h;
