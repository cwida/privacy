-- Naive PAC sample-and-aggregate (explicit sample-table join + pac_aggregate terminal).
-- Q30: SELECT SUM(ResolutionWidth), SUM(ResolutionWidth + 1), ..., SUM(ResolutionWidth + 89) FROM hits;
WITH samples AS (
    SELECT u.UserID AS pu_key, s.sample_id
    FROM (SELECT DISTINCT UserID FROM hits) u
    CROSS JOIN generate_series(1, 128) AS s(sample_id)
    WHERE random() < 0.5
),
per_sample AS (
    SELECT s.sample_id,
           COUNT(*) AS cnt,
           SUM(h.ResolutionWidth) AS sw0,
           SUM((h.ResolutionWidth + 1)) AS sw1,
           SUM((h.ResolutionWidth + 2)) AS sw2,
           SUM((h.ResolutionWidth + 3)) AS sw3,
           SUM((h.ResolutionWidth + 4)) AS sw4,
           SUM((h.ResolutionWidth + 5)) AS sw5,
           SUM((h.ResolutionWidth + 6)) AS sw6,
           SUM((h.ResolutionWidth + 7)) AS sw7,
           SUM((h.ResolutionWidth + 8)) AS sw8,
           SUM((h.ResolutionWidth + 9)) AS sw9,
           SUM((h.ResolutionWidth + 10)) AS sw10,
           SUM((h.ResolutionWidth + 11)) AS sw11,
           SUM((h.ResolutionWidth + 12)) AS sw12,
           SUM((h.ResolutionWidth + 13)) AS sw13,
           SUM((h.ResolutionWidth + 14)) AS sw14,
           SUM((h.ResolutionWidth + 15)) AS sw15,
           SUM((h.ResolutionWidth + 16)) AS sw16,
           SUM((h.ResolutionWidth + 17)) AS sw17,
           SUM((h.ResolutionWidth + 18)) AS sw18,
           SUM((h.ResolutionWidth + 19)) AS sw19,
           SUM((h.ResolutionWidth + 20)) AS sw20,
           SUM((h.ResolutionWidth + 21)) AS sw21,
           SUM((h.ResolutionWidth + 22)) AS sw22,
           SUM((h.ResolutionWidth + 23)) AS sw23,
           SUM((h.ResolutionWidth + 24)) AS sw24,
           SUM((h.ResolutionWidth + 25)) AS sw25,
           SUM((h.ResolutionWidth + 26)) AS sw26,
           SUM((h.ResolutionWidth + 27)) AS sw27,
           SUM((h.ResolutionWidth + 28)) AS sw28,
           SUM((h.ResolutionWidth + 29)) AS sw29,
           SUM((h.ResolutionWidth + 30)) AS sw30,
           SUM((h.ResolutionWidth + 31)) AS sw31,
           SUM((h.ResolutionWidth + 32)) AS sw32,
           SUM((h.ResolutionWidth + 33)) AS sw33,
           SUM((h.ResolutionWidth + 34)) AS sw34,
           SUM((h.ResolutionWidth + 35)) AS sw35,
           SUM((h.ResolutionWidth + 36)) AS sw36,
           SUM((h.ResolutionWidth + 37)) AS sw37,
           SUM((h.ResolutionWidth + 38)) AS sw38,
           SUM((h.ResolutionWidth + 39)) AS sw39,
           SUM((h.ResolutionWidth + 40)) AS sw40,
           SUM((h.ResolutionWidth + 41)) AS sw41,
           SUM((h.ResolutionWidth + 42)) AS sw42,
           SUM((h.ResolutionWidth + 43)) AS sw43,
           SUM((h.ResolutionWidth + 44)) AS sw44,
           SUM((h.ResolutionWidth + 45)) AS sw45,
           SUM((h.ResolutionWidth + 46)) AS sw46,
           SUM((h.ResolutionWidth + 47)) AS sw47,
           SUM((h.ResolutionWidth + 48)) AS sw48,
           SUM((h.ResolutionWidth + 49)) AS sw49,
           SUM((h.ResolutionWidth + 50)) AS sw50,
           SUM((h.ResolutionWidth + 51)) AS sw51,
           SUM((h.ResolutionWidth + 52)) AS sw52,
           SUM((h.ResolutionWidth + 53)) AS sw53,
           SUM((h.ResolutionWidth + 54)) AS sw54,
           SUM((h.ResolutionWidth + 55)) AS sw55,
           SUM((h.ResolutionWidth + 56)) AS sw56,
           SUM((h.ResolutionWidth + 57)) AS sw57,
           SUM((h.ResolutionWidth + 58)) AS sw58,
           SUM((h.ResolutionWidth + 59)) AS sw59,
           SUM((h.ResolutionWidth + 60)) AS sw60,
           SUM((h.ResolutionWidth + 61)) AS sw61,
           SUM((h.ResolutionWidth + 62)) AS sw62,
           SUM((h.ResolutionWidth + 63)) AS sw63,
           SUM((h.ResolutionWidth + 64)) AS sw64,
           SUM((h.ResolutionWidth + 65)) AS sw65,
           SUM((h.ResolutionWidth + 66)) AS sw66,
           SUM((h.ResolutionWidth + 67)) AS sw67,
           SUM((h.ResolutionWidth + 68)) AS sw68,
           SUM((h.ResolutionWidth + 69)) AS sw69,
           SUM((h.ResolutionWidth + 70)) AS sw70,
           SUM((h.ResolutionWidth + 71)) AS sw71,
           SUM((h.ResolutionWidth + 72)) AS sw72,
           SUM((h.ResolutionWidth + 73)) AS sw73,
           SUM((h.ResolutionWidth + 74)) AS sw74,
           SUM((h.ResolutionWidth + 75)) AS sw75,
           SUM((h.ResolutionWidth + 76)) AS sw76,
           SUM((h.ResolutionWidth + 77)) AS sw77,
           SUM((h.ResolutionWidth + 78)) AS sw78,
           SUM((h.ResolutionWidth + 79)) AS sw79,
           SUM((h.ResolutionWidth + 80)) AS sw80,
           SUM((h.ResolutionWidth + 81)) AS sw81,
           SUM((h.ResolutionWidth + 82)) AS sw82,
           SUM((h.ResolutionWidth + 83)) AS sw83,
           SUM((h.ResolutionWidth + 84)) AS sw84,
           SUM((h.ResolutionWidth + 85)) AS sw85,
           SUM((h.ResolutionWidth + 86)) AS sw86,
           SUM((h.ResolutionWidth + 87)) AS sw87,
           SUM((h.ResolutionWidth + 88)) AS sw88,
           SUM((h.ResolutionWidth + 89)) AS sw89
    FROM samples s JOIN hits h ON h.UserID = s.pu_key
    GROUP BY s.sample_id
)
SELECT
       pac_aggregate(array_agg(sw0 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw0,
       pac_aggregate(array_agg(sw1 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw1,
       pac_aggregate(array_agg(sw2 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw2,
       pac_aggregate(array_agg(sw3 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw3,
       pac_aggregate(array_agg(sw4 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw4,
       pac_aggregate(array_agg(sw5 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw5,
       pac_aggregate(array_agg(sw6 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw6,
       pac_aggregate(array_agg(sw7 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw7,
       pac_aggregate(array_agg(sw8 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw8,
       pac_aggregate(array_agg(sw9 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw9,
       pac_aggregate(array_agg(sw10 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw10,
       pac_aggregate(array_agg(sw11 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw11,
       pac_aggregate(array_agg(sw12 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw12,
       pac_aggregate(array_agg(sw13 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw13,
       pac_aggregate(array_agg(sw14 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw14,
       pac_aggregate(array_agg(sw15 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw15,
       pac_aggregate(array_agg(sw16 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw16,
       pac_aggregate(array_agg(sw17 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw17,
       pac_aggregate(array_agg(sw18 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw18,
       pac_aggregate(array_agg(sw19 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw19,
       pac_aggregate(array_agg(sw20 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw20,
       pac_aggregate(array_agg(sw21 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw21,
       pac_aggregate(array_agg(sw22 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw22,
       pac_aggregate(array_agg(sw23 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw23,
       pac_aggregate(array_agg(sw24 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw24,
       pac_aggregate(array_agg(sw25 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw25,
       pac_aggregate(array_agg(sw26 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw26,
       pac_aggregate(array_agg(sw27 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw27,
       pac_aggregate(array_agg(sw28 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw28,
       pac_aggregate(array_agg(sw29 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw29,
       pac_aggregate(array_agg(sw30 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw30,
       pac_aggregate(array_agg(sw31 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw31,
       pac_aggregate(array_agg(sw32 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw32,
       pac_aggregate(array_agg(sw33 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw33,
       pac_aggregate(array_agg(sw34 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw34,
       pac_aggregate(array_agg(sw35 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw35,
       pac_aggregate(array_agg(sw36 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw36,
       pac_aggregate(array_agg(sw37 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw37,
       pac_aggregate(array_agg(sw38 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw38,
       pac_aggregate(array_agg(sw39 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw39,
       pac_aggregate(array_agg(sw40 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw40,
       pac_aggregate(array_agg(sw41 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw41,
       pac_aggregate(array_agg(sw42 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw42,
       pac_aggregate(array_agg(sw43 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw43,
       pac_aggregate(array_agg(sw44 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw44,
       pac_aggregate(array_agg(sw45 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw45,
       pac_aggregate(array_agg(sw46 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw46,
       pac_aggregate(array_agg(sw47 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw47,
       pac_aggregate(array_agg(sw48 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw48,
       pac_aggregate(array_agg(sw49 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw49,
       pac_aggregate(array_agg(sw50 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw50,
       pac_aggregate(array_agg(sw51 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw51,
       pac_aggregate(array_agg(sw52 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw52,
       pac_aggregate(array_agg(sw53 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw53,
       pac_aggregate(array_agg(sw54 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw54,
       pac_aggregate(array_agg(sw55 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw55,
       pac_aggregate(array_agg(sw56 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw56,
       pac_aggregate(array_agg(sw57 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw57,
       pac_aggregate(array_agg(sw58 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw58,
       pac_aggregate(array_agg(sw59 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw59,
       pac_aggregate(array_agg(sw60 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw60,
       pac_aggregate(array_agg(sw61 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw61,
       pac_aggregate(array_agg(sw62 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw62,
       pac_aggregate(array_agg(sw63 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw63,
       pac_aggregate(array_agg(sw64 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw64,
       pac_aggregate(array_agg(sw65 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw65,
       pac_aggregate(array_agg(sw66 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw66,
       pac_aggregate(array_agg(sw67 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw67,
       pac_aggregate(array_agg(sw68 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw68,
       pac_aggregate(array_agg(sw69 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw69,
       pac_aggregate(array_agg(sw70 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw70,
       pac_aggregate(array_agg(sw71 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw71,
       pac_aggregate(array_agg(sw72 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw72,
       pac_aggregate(array_agg(sw73 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw73,
       pac_aggregate(array_agg(sw74 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw74,
       pac_aggregate(array_agg(sw75 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw75,
       pac_aggregate(array_agg(sw76 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw76,
       pac_aggregate(array_agg(sw77 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw77,
       pac_aggregate(array_agg(sw78 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw78,
       pac_aggregate(array_agg(sw79 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw79,
       pac_aggregate(array_agg(sw80 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw80,
       pac_aggregate(array_agg(sw81 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw81,
       pac_aggregate(array_agg(sw82 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw82,
       pac_aggregate(array_agg(sw83 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw83,
       pac_aggregate(array_agg(sw84 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw84,
       pac_aggregate(array_agg(sw85 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw85,
       pac_aggregate(array_agg(sw86 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw86,
       pac_aggregate(array_agg(sw87 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw87,
       pac_aggregate(array_agg(sw88 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw88,
       pac_aggregate(array_agg(sw89 ORDER BY sample_id), array_agg(cnt ORDER BY sample_id), 1.0/128, 3) AS sw89
FROM per_sample;
